# wp-docker-compose
Simple WordPress Docker Compose recipe

## Usage
```sh
docker compose up
```

## Directories
All relevant `wp-content` directories are exposed in host.
- `mu-plugins`
- `plugins`
- `themes`
